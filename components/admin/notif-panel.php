<!-- NOTIFICATION PANEL -->
  <div class="notif-panel" id="notifPanel">
    <div class="notif-header">
      <h4>Notifications</h4>
      <button onclick="markAllRead()" style="font-size:11px;color:var(--info);background:none;border:none;cursor:pointer;">Mark all read</button>
    </div>
    <div id="notifBody">
      <div style="padding:16px;font-size:12px;color:var(--text-muted)">Loading…</div>
    </div>
  </div>
